<?xml version="1.0" encoding="UTF-8"?>
<svg width="600" height="200" viewBox="125 60 531 160" xmlns="http://www.w3.org/2000/svg">

  <defs>
    <style>
      .text {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        font-weight: 700;
        font-size: 78px;
        fill: #FFFFFF;
        letter-spacing: -1.5px;
      }
    </style>
  </defs>

  <!-- Smaller Premium Lens Icon -->
  <g transform="translate(120,70) scale(0.75)">

    <!-- Outer housing -->
    <circle cx="90" cy="90" r="72"
            fill="none" stroke="#FFFFFF" stroke-width="10"/>

    <!-- Inner depth ring -->
    <circle cx="90" cy="90" r="48"
            fill="none" stroke="#2563EB" stroke-width="6"/>

    <!-- Subtle inner glass ring -->
    <circle cx="90" cy="90" r="28"
            fill="none" stroke="#FFFFFF" stroke-width="4" opacity="0.6"/>

    <!-- Center precision core -->
    <circle cx="90" cy="90" r="8"
            fill="#2563EB"/>

  </g>

  <!-- Wordmark (moved closer) -->
  <text x="255" y="165" class="text">
    Prop<tspan fill="#2563EB">Scope</tspan>
  </text>

</svg>
